#include <stdio.h>
#include <conio.h>

void main()
{
	int numInputs,sumInputs, outResult;
	
	//if any input is FALSE, the output is FALSE (= 0)
	if(sumInputs!=numInputs)
		outResult=0;
	else
		outResult=1;
}
