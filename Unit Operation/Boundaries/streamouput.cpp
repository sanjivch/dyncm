#include <iostream>
int main()
{
	double outletPres;
	double elevation;
	double fluidDensity;
	
	//Part of the solver. Flows will be defined based on the upstream objects!
	//all this is object has to do is define the pressure of the boundary and receive the flow give by the upstream connector
}
